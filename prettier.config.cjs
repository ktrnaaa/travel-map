/** @type {import("prettier").Config} */
// Це підказка для TypeScript/IDE про тип конфігурації (щоб працювала автопідказка)

module.exports = {
  semi: true, // Додавати крапку з комою в кінці рядків
  singleQuote: true, // Використовувати одинарні лапки замість подвійних
  tabWidth: 2, // Кількість пробілів для одного рівня відступу (2 пробіли)
  trailingComma: 'es5', // Додавати кому в кінці об'єктів/масивів у стилі ES5 (але не в параметрах функцій)
  printWidth: 100, // Максимальна довжина рядка — 100 символів (довші рядки переносяться)
  bracketSpacing: true, // Додавати пробіли всередині дужок об'єктів — { foo: bar } замість {foo:bar}
  arrowParens: 'avoid', // Не обгортати параметр стрілкової функції дужками, якщо він один (x => ...)
};
